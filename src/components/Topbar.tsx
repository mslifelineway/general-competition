import React from "react";

export const Topbar = () => {
  return (
    <section className="section-topbar">
      <h1 className="heading-primary heading-primary--title">Topbar Title</h1>
    </section>
  );
};
